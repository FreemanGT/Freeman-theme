# Infinite Scroll — Changelog

## 1.0.0
- Initial port from `bookomers-infinite-scroll` v1.0.5.
- Adds skeleton count, max pages, and end-message settings via Freeman admin.
- Debug flag renamed to `?freeman_is_debug=1` (legacy `?bookomers_debug=1` still works).
- Front-end CSS classes retained (`bookomers-*`) for customer overrides.
